function cont(curWindow, cWhite0)

Screen('DrawText', curWindow, '!!!We ate finished with the examples....!!!',500,300,cWhite0);
Screen('DrawText', curWindow, '...We will start the experiment now...',500,350,cWhite0);
Screen('Flip',curWindow);
WaitSecs(3);
